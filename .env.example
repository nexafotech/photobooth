# Environment
PORT=3001
NODE_ENV=development

# Session secret (change in production!)
SESSION_SECRET=change-me-to-a-random-string-in-production

# Data retention (days) — records older than this are auto-deleted on startup
RETENTION_DAYS=30
